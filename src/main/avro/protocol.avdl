@namespace("com.github.dfauth.socketio")
protocol Example {

  record Envelope{
    string id;
    map<string> metadata;
    string schemaRegistryTopic;
    bytes payload;
  }

  record TestEvent {
    string name;
    int ackId;
    string topic;
    string payload;
  }

}
